#!/bin/bash

# Display Hyprland keybindings in Walker for interactive search.
# Resolves keycodes to readable symbols via xkbcli.

declare -A KEYCODE_SYM_MAP

build_keymap_cache() {
    local keymap
    keymap="$(xkbcli compile-keymap 2>/dev/null)" || return 1

    while IFS=, read -r code sym; do
        [[ -z $code || -z $sym ]] && continue
        KEYCODE_SYM_MAP["$code"]="$sym"
    done < <(
        awk '
            BEGIN { sec = "" }
            /xkb_keycodes/ { sec = "codes"; next }
            /xkb_symbols/  { sec = "syms";  next }
            sec == "codes" {
                if (match($0, /<([A-Za-z0-9_]+)>\s*=\s*([0-9]+)\s*;/, m)) code_by_name[m[1]] = m[2]
            }
            sec == "syms" {
                if (match($0, /key\s*<([A-Za-z0-9_]+)>\s*\{\s*\[\s*([^, \]]+)/, m)) sym_by_name[m[1]] = m[2]
            }
            END {
                for (k in code_by_name) {
                    c = code_by_name[k]
                    s = sym_by_name[k]
                    if (c != "" && s != "" && s != "NoSymbol") print c "," s
                }
            }
        ' <<<"$keymap"
    )
}

parse_keycodes() {
    while IFS= read -r line; do
        if [[ $line =~ code:([0-9]+) ]]; then
            code="${BASH_REMATCH[1]}"
            symbol="${KEYCODE_SYM_MAP[$code]}"
            echo "${line/code:${code}/$symbol}"
        elif [[ $line =~ mouse:([0-9]+) ]]; then
            code="${BASH_REMATCH[1]}"
            case "$code" in
                272) symbol="LMB" ;;
                273) symbol="RMB" ;;
                274) symbol="MMB" ;;
                *)   symbol="mouse:${code}" ;;
            esac
            echo "${line/mouse:${code}/$symbol}"
        else
            echo "$line"
        fi
    done
}

dynamic_bindings() {
    hyprctl -j binds |
        jq -r '.[] | select(.submap == "") | "\(.modmask),\(.key)@\(.keycode),\(.description),\(.dispatcher),\(.arg)"' |
        sed -r \
            -e 's/null//' \
            -e 's,uwsm-app -- ,,' \
            -e 's,~/.local/bin/,,' \
            -e 's,~/.config/hypr/scripts/,,' \
            -e 's/@0//' \
            -e 's/,@/,code:/' \
            -e 's/^0,/,/' \
            -e 's/^1,/SHIFT,/' \
            -e 's/^4,/CTRL,/' \
            -e 's/^5,/SHIFT CTRL,/' \
            -e 's/^8,/ALT,/' \
            -e 's/^9,/SHIFT ALT,/' \
            -e 's/^12,/CTRL ALT,/' \
            -e 's/^64,/SUPER,/' \
            -e 's/^65,/SUPER SHIFT,/' \
            -e 's/^68,/SUPER CTRL,/' \
            -e 's/^69,/SUPER SHIFT CTRL,/' \
            -e 's/^72,/SUPER ALT,/' \
            -e 's/^73,/SUPER SHIFT ALT,/' \
            -e 's/^76,/SUPER CTRL ALT,/'
}

parse_bindings() {
    awk -F, '{
        key_combo = $1 " + " $2
        gsub(/^[ \t]*\+?[ \t]*/, "", key_combo)
        gsub(/[ \t]+$/, "", key_combo)

        action = $3
        if (action == "") {
            for (i = 4; i <= NF; i++)
                action = action $i (i < NF ? "," : "")
            sub(/,$/, "", action)
            gsub(/(^|,)[[:space:]]*exec[[:space:]]*,?/, "", action)
            gsub(/^[ \t]+|[ \t]+$/, "", action)
            gsub(/[ \t]+/, " ", key_combo)
        }

        if (action != "")
            printf "%-35s → %s\n", key_combo, action
    }'
}

# TODO: uncomment and adapt when menu system is built
# prioritize_entries() {
#     awk '{
#         line = $0; prio = 50
#         if (match(line, /Terminal/)) prio = 0
#         if (match(line, /Browser/)) prio = 2
#         if (match(line, /File manager/)) prio = 3
#         if (match(line, /Launch apps/)) prio = 4
#         if (match(line, /Full screen/)) prio = 8
#         if (match(line, /Close window/)) prio = 10
#         if (match(line, /Close all windows/)) prio = 11
#         if (match(line, /Lock system/)) prio = 12
#         if (match(line, /Toggle.*floating/)) prio = 13
#         if (match(line, /Pop window/)) prio = 15
#         if (match(line, /Clipboard/)) prio = 17
#         if (match(line, /Audio/)) prio = 18
#         if (match(line, /Bluetooth/)) prio = 19
#         if (match(line, /Screenshot/)) prio = 23
#         if (match(line, /workspace/)) prio = 27
#         if (match(line, /notification/)) prio = 37
#         if (match(line, /gaps/)) prio = 39
#         if (match(line, /nightlight/)) prio = 40
#         if (match(line, /XF86/)) prio = 99
#         printf "%d\t%s\n", prio, line
#     }' | sort -k1,1n -k2,2 | cut -f2-
# }

output_keybindings() {
    build_keymap_cache
    dynamic_bindings | sort -u | parse_keycodes | parse_bindings
}

if [[ $1 == "--print" || $1 == "-p" ]]; then
    output_keybindings
else
    monitor_height=$(hyprctl monitors -j | jq -r '.[] | select(.focused == true) | .height')
    menu_height=$((monitor_height * 40 / 100))

    output_keybindings |
        walker --dmenu -p 'Keybindings' --width 800 --height "$menu_height"
fi
